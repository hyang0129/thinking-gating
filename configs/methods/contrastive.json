{
  "name": "contrastive",
  "type": "probe",
  "architecture": "contrastive_mlp",
  "description": "Contrastive learning on prefill states (thinking-helped vs not-helped)",
  "params": {
    "input_dim": 4096,
    "embedding_dim": 128,
    "hidden_dims": [256, 128],
    "temperature": 0.07,
    "dropout": 0.1
  },
  "training": {
    "optimizer": "adam",
    "learning_rate": 1e-3,
    "batch_size": 32,
    "max_epochs": 50,
    "early_stopping_patience": 10,
    "validation_split": 0.2,
    "contrastive_pairs": "hard_negatives"
  },
  "notes": "Contrastive version from HalluLens. May achieve slightly better separation if label distribution is skewed."
}
